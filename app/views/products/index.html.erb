<% cart.each do |p| %>
  <p><%= p %></p>
<% end %>

<%= form_for :product, :html => {url: products_add_path} do |f| %>
  <%= f.label :product %> <%= f.text_field :product %>
  <%= f.submit "add to cart", {id: "add to cart"}%>
<% end %>
